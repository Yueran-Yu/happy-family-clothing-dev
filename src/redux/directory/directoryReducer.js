import SECTIONS_DATA from "./Sections_Data";

const directoryReducer = (state = SECTIONS_DATA, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default directoryReducer;

